# mysqld_exporter configuration file

# Data source used by mysqld_exporter to connect to database server.
# Replace username, password and localhost with your infrastructure information.
# Additionally, this file must be owner by root:root and mode 0600

DATA_SOURCE_NAME="username:password@(localhost:3306)/"

# The following definitions are the default values that appear in 
# /usr/lib/systemd/system/prometheus-mysqld-exporter.service.d/environment.conf
#
# Uncomment to override the default values and customize mysqld_export

# Address on which to expose metrics and web interface. (default ":9104")
#WEB_ADDR=0.0.0.0:9104

# Path under which to expose metrics. (default "/metrics")
#TELEMETRY_PATH=/metrics

# Log format
#LOG_FORMAT=logger:syslog?appname=mysqld_exporter&local=2

# Set logging level. Default is info. Only log messages with the given severity or above.
# Valid levels: [debug, info, warn, error, fatal]
#LOG_LEVEL=warn
